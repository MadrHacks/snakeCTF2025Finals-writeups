# Example Challenge [_snakeCTF 2024 Quals_]

**Category**: category

## Description

Here is the description of the challenge as it is on CTFd.

### Hints

- This is hint given during the CTF for this challenge.
- This is the second hint cause the challenge was impossible to solve.

## Solution

Here is the solution of the challenge. The solution _can_ be divided into multiple steps.

The writeup MUST follow these guidelines:

1. The writeup MUST be written in Markdown;
2. The writeup MUST be written in [British English](https://en.wikipedia.org/wiki/British_English), using [impersonal passive voice](https://en.wikipedia.org/wiki/Impersonal_passive_voice);
3. The writeup MUST be named `README.md`;
4. The writeup MUST be in the `src/writeup` directory of the challenge;
5. All the code MUST be in the same directory as the writeup;
6. The (commented) code for the solver and other attachments (if any, see below) MUST be in the `attachments` directory;
7. The writeup images MUST be in the `images` directory;
8. The writeup MUST contain the following sections:
   - Title, CTF name and Category
   - Description
   - Solution

Note that the challenge attachments (e.g. built with `build-attachments.sh`) will be added later, so do not include them in the writeup directory.

### First step

Here is the first step of the solution.

### Second step

Here is the second step of the solution.

[Here](./attachments/solve.py) is the solver code.

```python
# Python code
print("Hello, World!")
```

### Third step

![image](./images/image.png)
